# 활성 프롬프트 버전 (BR-93, NFR-22, CP-2)
#
# 프롬프트를 코드에 문자열로 두지 않는 이유는 두 가지다.
#   1. 버전을 식별할 수 있어야 한다 — `llm_call.prompt_version` 에 기록되므로,
#      나중에 "품질이 언제부터 나빠졌나"를 프롬프트 변경과 대조할 수 있다 (u4 회귀 분석).
#   2. 프롬프트 변경은 코드 변경보다 자주 일어나고, 리뷰의 성격도 다르다.
#
# 버전은 semver 다. 지시의 의미가 바뀌면 minor 이상을 올린다 — 오탈자 수정만 patch.

answer: 1.0.0
verify: 1.0.0
entity: 1.0.0
# u4 - 채점자. 답변 모델과 다른 모델로 호출된다 (BR-122).
judge: 1.0.0
