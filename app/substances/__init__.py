"""Substance master projection and safety cards (u3-substance-card).

A *responsibility* boundary, not a unit boundary (UD-4). The projection in
`projection.py` belongs to u1's rules (BR-33, BR-37) and lives here only because
that is where the substance domain now sits.
"""
