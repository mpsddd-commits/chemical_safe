# 작업 백로그 — 커밋 단위로 쪼갠 남은 일

**작성**: 2026-09-02
**용도**: 하루 약 10커밋의 재료. **각 항목이 커밋 하나**가 되도록 크기를 맞췄다.
**원칙**: 빈 커밋·날짜 소급·의미 없는 분할은 넣지 않는다. 여기 있는 것은 전부
실제로 해야 하는 일이고, 안 해도 되는 일은 "안 함"으로 적었다.

우선순위는 **측정된 영향**이 정한다. 지표를 움직이지 않는 항목은 그렇다고 적었다.

---

## 상태 (2026-09-02 기준)

```
기준선   385 retrieval_only  R@5 0.967 · MRR 0.766 · 거부 1.000 · 오거부 0.000
        276 full            정답률 0.917 · 인용 0.610 · 충실도 0.958
코퍼스   82문서 · 1,712청크 · 물질 49종
테스트   단위 677 · 통합 72 · ruff clean
```

---

## A. 진행 중 — 내일 자동 재개 (커밋 2~3개)

| # | 항목 | 비고 |
|---|---|---|
| A1 | `evaluate --resume 387` 완주 → `--promote 387` | 예약됨(9/3 16:07). 심판 할당량 하루치 |
| A2 | 갱신된 `full` 기준선을 문서 3종에 반영 | audit · state · operations 기준선 표 |
| A3 | 충실도 0.958 → 1.000 회복 여부 판정 기록 | law-09 진단이 맞았는지의 답 |

---

## B. 보안 — 공개 저장소에 적힌 미충족 (커밋 8~12개)

README 가 이미 "인증 없음"으로 고지하고 있다. 고지가 있다고 결함이 아닌 것은 아니다.

| # | 항목 | 근거 |
|---|---|---|
| B1 | `/admin` 인증 요구 (`require_user`) | 익명 200 실측. 통제가 `127.0.0.1` 바인딩뿐 |
| B2 | `/usage` 인증 요구 | 익명에게 호출·비용 노출. u5 문서가 "로그인만"이라 **잘못 적었던** 곳 |
| **B2a** | **`/api/*` 인증 요구 — 우선순위 최상** | 2026-09-02 실측: `/api/sources`·`/api/jobs`·`/api/stats` 익명 200, **`POST /api/sources/{id}/ingest` 도 인증 없이 도달**(409 는 업무 응답이지 인증 거부가 아니다). HTML 화면보다 위험이 크다 — 읽기가 아니라 **상태 변경**이다 |
| B3 | 역할 개념 도입 (`admin` / `user`) | FR 에 없어 만들지 않았다. B1·B2 를 제대로 하려면 필요 |
| B4 | B1~B3 통합 테스트 | 익명·일반·관리자 각각의 결과 고정 |
| B5 | 토큰 무효화 (서버 측 폐기 목록) | 탈취 토큰이 **최대 12시간** 유효. AP-3 의 대가 |
| B6 | 비밀번호 재설정 | 미구현. FR 에 없다 |
| B7 | 계정 삭제 + 소유 문서 처리 정책 | `document.owner_id` 에 FK 가 없는 이유와 함께 결정 |
| B8 | 업로드 용량 초과 메시지 반올림 | 200MB 에선 무해하나 작은 값에서 "0MB / 0MB" |

---

## C. 품질 — 지표를 움직이는 것 (커밋 6~10개)

| # | 항목 | 예상 영향 |
|---|---|---|
| C1 | 아크릴산(79-10-7) 국문 MSDS 수집 | 카드 0/7 결측 4건 → 3건. 국문 PDF 미발견 상태 |
| C2 | MSDS 추가 수집 (카드 6/7 결측 41건) | 카드 커버리지. **코퍼스 문제이지 코드 문제가 아니다** |
| C3 | BR-31a ↔ BR-104 충돌 | 짧은 문서의 섹션이 접혀 카드가 못 읽는다 (49건 중 1건) |
| C4 | `FINAL_TOP_K` 와 Recall@10 | 후보가 5건이라 Recall@10 이 **구조적으로 측정 불가**(결함 49) |
| C5 | 한국어 형태소 분석 (`pg_bigm` 등) 검토 | 지금은 조사 절단 + 제목 부분일치로 우회 |
| C6 | 리랭커 재검토 | CPU 2,121~2,830ms vs 예산 1,200ms. 켤 수 없다 |

---

## D. 정리 — 사문화·부채 (커밋 5~8개)

| # | 항목 | 판단 |
|---|---|---|
| D1 | `doc_type_hint` 처분 | u2 가 만들고 4개 유닛 지나도록 **소비자가 없다**. 구현하거나 제거 |
| D2 | `meta.substance_names` 처분 | 1,712청크 전부 빈 배열. 색인이 채우지 않는다 |
| D3 | `pip install -e .` 패키징 | 최상위 디렉터리 다수로 setuptools 자동 탐색 실패. **`pyproject.toml` 은 빌드 지문에 포함되므로 승격 대기 중에는 건드리지 말 것** |
| D4 | `.gitattributes` 추가 (`* text=auto eol=lf`) | **단순 정리가 아니다.** `core.autocrlf=true` + 혼재된 작업 트리(app/ LF, pyproject.toml CRLF)라 체크아웃 한 번이 빌드 지문을 바꾼다 — 2026-09-03 실측: LF 113파일이 CRLF 가 되면 `03bab957bade` → `7b008836585f`. 그러면 코드를 안 고쳤는데 승격이 막힌다. **진행 중 평가의 승격이 끝난 뒤에 할 것** — 이 작업 자체가 재정규화를 일으킨다 |
| D5 | `safeenv/` 백업 폴더 정리 | 이전 완료 확인 후. 지금 지우면 되돌릴 곳이 없다 |
| D6 | README 스크린샷·아키텍처 다이어그램 | 공개 저장소의 첫인상 |

---

## E. 판단 대기 — 사람이 정할 것 (커밋 아님)

| # | 항목 | 상태 |
|---|---|---|
| E1 | sub-02 골든셋 근거 | **B 유지 결정됨** (2026-09-01). 재론 시에만 |
| E2 | sub-07 골든셋 근거 | **B 유지 결정됨** (2026-09-01) |
| E3 | MSDS PDF 재배포 조건 | 제조사 공개 문서이나 법적 확인은 안 했다 |
| E4 | 초기 커밋 분할 여부 | 아래 참조 |

---

## 커밋 규약

```
<영역>: <무엇을 바꿨는가>

무엇을 측정했고 왜 그렇게 했는지. 지표가 움직였으면 숫자를 적는다.
움직이지 않았으면 그것도 적는다.
```

영역: `ingest` `index` `rag` `eval` `auth` `web` `docs` `ci` `ops`

예:
```
rag: 조문 제목 층으로 법령 질의 근접 실패 해소

본문 prefix 매칭 5개 변형을 먼저 재고 전부 기각했다 — ts_rank 재편은
제로섬이라 매번 기존 문항을 잃었다(law-04·msds-03 1.000→0.000).
section_title LIKE 는 조사 문제가 구조적으로 없다.

Recall@5 0.867 → 0.933 · MRR 0.630 → 0.708 · Recall 회귀 0
```

**하지 않는 것**: 빈 커밋, 커밋 수를 맞추기 위한 인위적 분할,
`GIT_AUTHOR_DATE` 소급. 이력은 실제로 한 일을 적는 곳이다.

---

## 초기 커밋 분할에 대하여 (E4)

`95677a9` 가 392파일을 한 커밋으로 담아 **이미 푸시됐다.** 이를 40~60개의
의미 있는 커밋으로 쪼개려면 이력 재작성 + `--force` 푸시가 필요하다.

| | 이득 | 대가 |
|---|---|---|
| 분할 | u1~u5 와 운영 개선의 구조가 이력에 남는다 | **푸시된 이력 재작성.** 개인 저장소이고 협업자가 없어 위험은 낮지만 되돌릴 수 없다 |
| 유지 | 안전 | 첫 커밋이 "전부"라 이력이 설계를 설명하지 못한다 |

**권고: 유지.** 앞으로의 작업이 이력을 만든다 — 위 백로그만으로 25~35커밋이
있고, 그것이 실제로 한 일의 기록이다. 분할을 원하면 별도로 지시할 것.
