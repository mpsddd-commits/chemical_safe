# 작업 백로그 — 커밋 단위로 쪼갠 남은 일

**작성** 2026-09-02 · **갱신** 2026-09-10 (C9·C13·C14·C15 완료 후 재정리)
**용도**: 각 항목이 **커밋 하나**가 되도록 크기를 맞춘 실행 목록.
**원칙**: 빈 커밋·날짜 소급·의미 없는 분할은 넣지 않는다. 하루 10커밋은 목표가
아니라 백로그를 소화한 결과다 — 재료가 없는 날은 안 나온다(9/3 이 그랬다).

우선순위는 **측정된 영향**이 정한다. 지표를 움직이지 않는 항목은 그렇다고 적었다.

> 세션이 없어도 진행할 수 있다. 순서와 명령은
> `operations/runbook-next-batch.md`, 스크립트는 `scripts/`.

---

## 상태 (2026-09-10 실측)

```
기준선   615 full  (2026-09-10 승격, 코퍼스 97/1,954, 지문 d80dbd0cb31d)
        R@5 0.967 · R@10 1.000 · MRR 0.801 · 정답률 1.000(심판 25) ·
        인용 0.799 · 충실도 1.000 · 거부 1.000 · **오거부 0.000**
        quota_exhausted 0 — 실행 중 할당량 실패 0건, 이번엔 진짜다

        첫 깨끗한 완주다. 609 에서 거부됐던 msds-03·sub-04·sub-06 이 전부
        답변으로 돌아섰고 셋 다 정답 판정을 받았다. 심판 분모도 22 → 25 로
        답변형 전체를 덮었다.

코퍼스   97문서 · 1,954청크 · 물질 49종 · 계정 1
테스트   단위 797 · 통합 113 · ruff clean
지문     d80dbd0cb31d
```

**검색은 갈 데까지 갔다.** R@10 1.000 은 검색이 근거를 놓친 적이 없다는 뜻이고,
남은 0.033 은 검색 실패가 아니라 순위 문제다. 이번 주에 닫은 것들(C9·C13·C14·C15)은
새 기능이 아니라 **측정을 믿을 수 있게 만드는 일**이었다 — 거부가 진단 자료를
지우던 것, 할당량 중단이 제품 실패로 집계되던 것, 프롬프트가 근거의 이름을
빼먹던 것.

---

## 완료 — 지우지 않고 기록으로 남긴다

| # | 항목 | 결과 |
|---|---|---|
| ~~A1~A3~~ | 실행 387 완주·승격 | 2026-09-04. 첫 시도 30/30. **law-09 진단 적중** — 불충실·오답이 충실·정답으로 회복 |
| ~~B1~~ | `/admin` 인증 | 익명 200 → **303**. `require_admin` |
| ~~B2~~ | `/usage` 인증 | 익명 200 → **303** |
| ~~B2a~~ | `/api/*` 인증 | 익명 200 → **401**. `POST ingest` 도 401 — 수집 실행이 닫혔다 |
| ~~B3~~ | 역할 도입 | 마이그레이션 `0006_role`. **역할은 토큰이 아니라 DB 에서** — 강등이 12시간 늦지 않게. CLI `grant-admin` |
| ~~B4~~ | 인증 통합 테스트 | 경로 × (익명/일반/관리자) 고정. 통합 72 → 108 |
| ~~D4~~ | `.gitattributes` | LF 고정. 73파일 정규화, 지문 `9b28088f237a → d9fdd7511c15` |
| ~~D5~~ | `safeenv/` 정리 | 삭제 완료. 운영 로그는 `logs/safeenv-pre-migration.log` 로 보존 |
| ~~C2~~ | MSDS 추가 수집 | **2026-09-07 완료.** 대정화금에서 15건 추가. 문서 82→97 · MSDS 12→**27(전부 structured)** · 청크 1,712→1,954 · MSDS 없는 물질 42→27. 대상 42종 대조 → 17종 적중 → **프로덕션 코드**(extract→normalize→find_sections)로 15/15 structured 확인 후 투입. 탈락 2건은 PDF 가 글자를 이중 렌더해 헤더가 안 잡힌다(D7) |
| ~~C1~~ | 아크릴산(79-10-7) 국문 MSDS | **2026-09-07 완료.** 8/30 에 '없다'고 적었던 것은 틀렸다 — 없었던 게 아니라 **그때 쓴 검색 경로가 닿지 않았다.** 대정화금에 있었다(SEQ 4021, 11쪽, 16항목) |

---

## 남은 것 — 재우선순위

검색도 답변도 실측 기준으로는 끝났다. **지금 1순위는 기능이 아니라 측정의
신뢰성이다** — 이번 주에 닫은 넷은 전부 "지표가 거짓말하던 것"이었고, 남은
C8 은 그 뒤에야 잴 수 있는 값이다.

### 1순위 — 측정을 믿을 수 있게 (대부분 완료)

| # | 항목 | 왜 여기인가 |
|---|---|---|
| ~~C9~~ | 정확일치 예산을 한 문서가 통째로 먹는다 | **2026-09-07 완료**(`2bb75fe`). `lookup_exact()` 가 정렬 없는 LIMIT 이라 황산 MSDS 두 건(16+14=30청크)이 `keyword_top_k` 를 정확히 채워 물질레코드가 통째로 밀려났고 sub-01 이 1.000 → 0.000. 문서 단위 라운드로빈 + 타이핑된 CAS 경로의 섹션 라우팅으로 복구. 실측 R@5 0.967 · R@10 1.000 · MRR 0.762 → 0.801 |
| **C8** | **거부 경계 흔들림 — 원인 규명됨, (b) 허용오차 재교정만 남음** (`operations/refusal-stability-review.md`) | 착수 시 원인을 표집으로 보고 온도 0 을 고정했으나(C8-a) 흔들림이 남았고, 2026-09-08~09 에 **진짜 원인이 밝혀졌다** — 근거에 물질 이름이 없어 생성기가 답하기를 거절하고(C13) 검증기가 익명 근거로 이름 있는 주장을 검사하던 것(C14). 둘 다 닫혔다. **(b) 허용오차 재교정은 아직**이고, 이제는 노이즈가 줄어든 상태에서 다시 재야 한다. 실행 615 완주가 그 첫 자료다 |
| ~~C15~~ | 할당량 중단이 '근거 불충분' 으로 기록된다 | **2026-09-08 완료.** `QueryResult.quota_blocked` 신설로 검증기가 삼킨 할당량 오류가 위로 전달된다. 걸러진 문장이 **전부** 차단이면 `verification_unavailable` 로 구별(일부만이면 최소 한 문장은 판정받았으므로 기존 사유 유지). **핵심은 평가 쪽** — 차단이 하나라도 있으면 그 문항은 `QUOTA_EXHAUSTED` 로 저장돼 모든 지표에서 빠지고 실행이 `partial` 로 끝난다. 멈추지 않고 계속한다: 609 의 msds-02 가 한 질의 안에서 5실패 8성공이라 분당 제한이고, 첫 실패에 멈추면 이틀치 실행을 1분이면 풀릴 조건으로 버린다. 화면 헤드라인도 사유별로 갈랐다 — 이전에는 "근거를 찾지 못했습니다" 다음에 "질문이나 자료의 문제가 아니라"가 붙어 화면이 스스로를 반박했다 |
| ~~C14~~ | 검증기가 원문과 거의 같은 문장을 쳐낸다 | **2026-09-09 완료 — 원인은 C13 과 같았다.** 진단(질의 492): 세 문장이 전부 같은 청크 17518(올바른 암모니아 근거)을 인용했고 청크 107자·스냅샷 107자로 잘림도 없었다 → 가설 (A) 생성기 인용 오류는 닫힘, **(B) 검증기 쪽**. 원인은 `for_verification` 이 근거에 `title` 을 안 넣는 것이었다 — 문장은 '암모니아'를 주장하는데 근거 텍스트에는 암모니아라는 말이 없고 이름은 제목에만 있다. **어제 생성기 프롬프트만 고치고 검증기 프롬프트는 안 고쳤다.** 제목을 넣자 질의 493 에서 5문장 전부 `supported`(492 는 3문장 중 1통과 2제외). `section` 은 넣지 않았다 — 제목은 근거의 **정체**지만 섹션 코드는 **주제에 대한 주장**이라, 본문에 없는 서술을 코드로 메울 여지를 준다. 제목은 '누구'를 풀고 '무엇'은 본문만 말해야 한다 |
| ~~C13~~ | 생성기가 근거의 물질을 모른다 | **2026-09-08 완료.** 제목 70건(substance 49·incident 12·law 9)을 채웠다. 재색인 불필요 — title 은 청크 메타에 없고 질의 시점 조인이다. **sub-06 이 3/3 거부 → 3/3 답변.** 검색은 자리 하나까지 불변(610→611). 어댑터도 함께 고쳐 다음 수집에서 다시 비지 않는다 |
| ~~C11~~ | 문장별 검증 임계 | **전제가 틀렸다.** 조정할 임계가 없다(LLM 이진 판정). 파생된 실제 결함 — 거부가 문장을 저장하지 않아 진단이 불가능했던 것 — 은 2026-09-08 고쳤다(BR-88 위반). 원인은 C13 |
| ~~C12~~ | sub-06 은 고정된 경계선 | **원인 밝혀짐 — C13.** 흔들림이 아니라 이름 없는 근거가 상위를 지배할 때 나타나는 결정적 결과였다 |
| ~~C7~~ | ~~sub-07·sub-08 충실도 하락~~ **완료 2026-09-06 — 충실도 0.917 → 1.000, 진단 적중** | 2026-09-04 원인: 심판이 문장별 인용마다 같은 스니펫을 받아 6,000자 상한에서 잘렸고, 실제로 존재하는 근거를 못 봤다. 중복 제거로 sub-07 9,007→3,732자·sub-08 7,565→1,235자. **2026-09-05 실행 549 에서 두 문항 모두 충실 true 로 회복.** 남은 것은 집계 지표 확인뿐 — 실행 549 가 19/30 에서 할당량으로 멈췄다(내일 심판 6콜). 옛 설명: | 387 에서 둘 다 `answered_partial` 이 되며 충실도가 0.958 → 0.917 로 내려갔다. **유일하게 나빠진 지표**이고, 검색을 고치자 답변 쪽에서 드러난 것이라 원인이 새롭다. 여러 변경이 겹쳐 원인 분해가 안 되므로 문항 단위로 근거·생성문장·검증결과를 직접 읽어야 한다 |
| ~~C4~~ | `FINAL_TOP_K` 와 Recall@10 | **2026-09-07 결정·구현**(`operations/recall-at-10-review.md` 6절). (b) 융합 중간 결과 관측 — Recall@5 를 잃지 않고 Recall@10 이 재진다. 실측 R@5 0.967 불변 · R@10 1.000 · MRR 0.766 → 0.772. `config` 에 `observation_top_k` 가 들어가 기준선 385 대비 `incomparable` 이며 그것이 옳다 |

### 2순위 — 코퍼스 (커밋 1개)

| # | 항목 | 왜 |
|---|---|---|
| C3 | BR-31a ↔ BR-104 충돌 | 짧은 문서의 섹션이 접혀 카드가 못 읽는다. 49건 중 **1건** — 영향이 작다 |

### 3순위 — 보안 잔여 (커밋 4개)

기본은 닫혔다. 남은 것은 **알려진 대가**이지 열린 구멍이 아니다.

| # | 항목 | 왜 지금이 아닌가 |
|---|---|---|
| B5 | 토큰 무효화 | 탈취 토큰 최대 12시간(AP-3). 관리자 강등은 B3 이 이미 즉시 반영한다 — 남은 것은 신원 자체의 무효화 |
| B7 | 계정 삭제 + 소유 문서 정책 | `document.owner_id` 에 FK 가 없는 이유와 함께 정해야 한다. 계정이 0개라 급하지 않다 |
| B6 | 비밀번호 재설정 | FR 에 없다. 계정이 0개다 |
| B8 | 업로드 용량 초과 메시지 반올림 | 200MB 한도에선 무해. 작은 값에서 "0MB / 0MB" |

### 4순위 — 부채·정리 (지표를 안 움직인다)

| # | 항목 | 비고 |
|---|---|---|
| D1 | `doc_type_hint` 처분 | u2 가 만들고 5개 유닛 지나도록 **소비자가 없다**. 구현하거나 제거 |
| D2 | `meta.substance_names` 처분 | **1,954청크 전부 빈 배열**(2026-09-10 실측). C13 이 이 필드의 용도를 확인해 줬다 — 근거의 물질 이름이 없어 생성기·검증기가 둘 다 막혔던 것이 그 결함이었다. 지금은 `document.title` 로 우회했고, 이 필드를 채우면 검색·생성·인용이 한 출처를 쓰게 된다. 다만 **전체 재색인이 필요하다** |
| D3 | `pip install -e .` 패키징 | 최상위 디렉터리 다수로 setuptools 자동 탐색 실패. **`pyproject.toml` 이 지문 대상이라 승격 대기 중에는 금지** |
| ~~D6~~ | README 스크린샷·다이어그램 | **2026-09-08 완료.** 동작 다이어그램(mermaid, 렌더 확인) + 화면 2장 — 인용이 달린 답변과 근거 부족 시 거부. 거부 화면을 넣은 이유는 그것이 이 제품의 특징이기 때문이다. 캡처는 헤드리스 크롬에 `POST /query` 자동 제출 폼을 물려 잡았다 — 서버 렌더 경로가 있어 가능했다 |
| ~~D10~~ | 거부 문구가 상황을 잘못 말한다 | **2026-09-10 완료.** 옛 문구는 두 번 틀렸다 — "어떤 물질인지 확인하지 못했습니다"는 카드뮴이라고 분명히 적은 사용자에게 거짓이고, "물질명이나 CAS 를 적어 주시면 답변합니다"는 **지킬 수 없는 약속**이었다(카드뮴은 코퍼스에 없다). **사유를 둘로 쪼개지 않았다** — `_match_synonyms` 가 해석된 이름만 담으므로 '안 지목함'과 '지목했으나 없음'이 코드에서 구별되지 않는다. 대신 시스템이 아는 것만 말하게 했다 |
| ~~D11~~ | 이명 테이블이 얇다 | **2026-09-13 완료.** 이명 98행은 사실 이명이 아니었다 — 물질마다 `name_ko`·`name_en` 두 개뿐이었다. **코퍼스에서만** 뽑아 45행 추가(MSDS 제목 · 섹션 3 의 관용명·이명 · 본문에 실제로 나오는 화학식). 지어낸 것 없음 — `유산` 은 코퍼스에 없어 넣지 않았다. 보수적 배제 규칙이 실제로 위험한 것을 걸렀다: koreachem 황산 섹션 1 에 **다이싸이온산**(다른 화합물)이 이명으로 적혀 있어 섹션 1 은 읽지 않고, 그 물질의 CAS 행에서만 뽑는다. doc 732 는 `화학물질명` 칸이 **다른 물질**(부틸말론산디에틸에스테르)을 적고 있는데 CAS 행 규칙이 그것을 걸렀다. **함께 막은 함정**: `replace_synonyms` 가 유형 무관 전부 지워 **재색인하면 백필이 조용히 사라졌다** — 이제 호출부가 `owned_types` 를 명시해야 하고 자기 유형만 교체한다. 화면의 '이명 검색 미지원' 도 **부분 지원**으로 정직하게 고쳤다(숫자 없이, 실행 시점에 센다). 검색 지표 불변 |
| D7 | 글자 이중 렌더 PDF | 아세탈·아이소펜테인 MSDS 가 모든 글자를 두 번 그린다(`11..화화학학제제품품`). `normalize()` 에 중복 제거를 넣으면 고쳐지지만 `1000`을 `10`으로 만들어 **멀쩡한 문서를 망가뜨린다.** 문서 2건 때문에 코퍼스 전체를 걸 일이 아니다 |
| D8 | 정책 확인이 엉뚱한 호스트를 본다 | `policy_check` 가 소스의 `base_url`(`msds.kosha.or.kr`)만 보고 `decision=unknown` 을 남긴다. **실제 PDF 는 전부 다른 호스트에 있다** — 지금은 사람이 호스트별 robots.txt 를 직접 확인해 막고 있다(삼전순약 제외가 그 결과). 검사가 실제 대상과 어긋나 있다 |
| ~~D9~~ | `ingest` 가 읽기 전용 원본에서 조용히 진행된다 | **2026-09-13 완료.** 수집 작업 맨 앞에서 원본 루트가 쓰기 가능한지 **실제 임시 파일로 한 번** 확인하고, 안 되면 아무것도 가져오기 전에 거부한다("수집은 worker 컨테이너에서 실행하십시오"). 문서 단위 저장 실패의 관용은 유지했다 — 일시 오류 하나로 수집 전체를 망치면 안 된다. 막는 것은 **전부가 실패하는 체계적 조건**뿐이다. 수집 진입점 4개(CLI·관리 화면·API·arq)가 전부 지나는 `IngestionService.execute` 에서 막았고, 재색인은 읽기만 하므로 검사하지 않는다. 실측: app 컨테이너에서 `ingest --source msds_pdf` → 거부·exit 2, MSDS 문서 수와 수정 시각 **불변** |
| C5 | 한국어 형태소 분석 검토 | 지금은 조사 절단 + 제목 부분일치로 우회 중이고 **잘 동작한다**(0.967). 바꿀 이유가 약하다 |
| C6 | 리랭커 재검토 | CPU 2,121~2,830ms vs 예산 1,200ms. 하드웨어가 바뀌기 전엔 못 켠다 |

### 판단 대기 — 사람이 정할 것 (커밋 아님)

| # | 항목 | 상태 |
|---|---|---|
| E1·E2 | sub-02·sub-07 골든셋 근거 | **B 유지 결정됨**(2026-09-01) |
| E3 | MSDS PDF 재배포 조건 | 제조사 공개 문서이나 법적 확인은 안 했다. **2026-09-07 에 범위가 12건 → 27건으로 커졌다**(대정화금 15건). 공개 저장소 `data/originals/msds_pdf/` 에 함께 담기며, 그 이유는 인증키 없이 재색인만으로 코퍼스를 복원할 수 있게 하기 위함이다(FQ-8=A). 판단이 '담지 않는다'로 나면 원본을 빼고 README 의 복원 약속을 함께 고쳐야 한다 — 둘은 한 몸이다 |
| E4 | 초기 커밋 분할 | **유지 권고.** 푸시된 이력 재작성이 필요하고, 앞으로의 작업이 이력을 만든다 |
| ~~E5~~ | 관리자 계정 생성 | **2026-09-04 완료.** `mpsddd@gmail.com` 을 관리자로. 종단 확인: 익명 303/401 · 관리자 200(`/admin`·`/admin/sources`·`/admin/jobs`·`/usage`·`/api/stats`), nav 4개 → 8개. **비밀번호 재설정이 없으므로(B6 미구현) 분실 시 계정을 지우고 다시 만들어야 한다** |

---

## 남은 분량

```
지금 착수 가능   C3 · D10 · D8 · D9 · D1 · D3          6커밋
보안 잔여        B5 · B6 · B7 · B8                     4커밋 (알려진 대가, 계정 1개)
자료 대기        C8(허용오차 재교정) — 615 완주가 첫 자료
재색인 필요      D2 (약 35분, 코퍼스 지문이 바뀐다)
안 한다(이유 있음) D7 · C5 · C6
사람이 정할 것   E3 · E4
```

**착수 시점(25~35커밋)에서 대부분 소화됐다.** 남은 것 중 **지표를 움직이는 것은
거의 없다** — 검색은 R@10 1.000 이고 답변 쪽 결함 넷(C9·C13·C14·C15)이 닫혔다.
D10 만이 사용자가 실제로 오해하는 문제이고, 나머지는 부채이거나 의도적으로
미룬 것이다.

**순서**: 615 완주·승격 → D10 → E3 판단. 그다음은 급한 것이 없다.

---

## 커밋 규약

```
<영역>: <무엇을 바꿨는가>

무엇을 측정했고 왜 그렇게 했는지. 지표가 움직였으면 숫자를,
안 움직였으면 그것도 적는다.
```

영역: `ingest` `index` `rag` `eval` `auth` `web` `docs` `ci` `ops`

**하지 않는 것**: 빈 커밋, 커밋 수를 맞추기 위한 인위적 분할,
`GIT_AUTHOR_DATE` 소급. 이력은 실제로 한 일을 적는 곳이다.
