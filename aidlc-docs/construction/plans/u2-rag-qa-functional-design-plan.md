# Functional Design Plan — u2-rag-qa

**프로젝트**: safeenv
**단계**: 🟢 CONSTRUCTION / Functional Design — 유닛 `u2-rag-qa` (2/5)
**작성일**: 2026-08-24
**상태**: 답변 확정 (권장안 / FQ2-4 = b) — 산출물 4종 생성

---

## 답변 방법

**2부 설계 질문(FQ2-1 ~ FQ2-20)** 의 `[Answer]:` 태그 뒤에 선택지 문자를 적어 주세요.
*(권장)* 표시는 요구사항·설계 결정 및 **u1 실측치**에 비추어 유리한 선택지입니다.

**"권장안대로 진행"** 이라고 하시면 *(권장)* 선택지로 채운 뒤 설계 산출물 4종을 생성합니다.

---

## 유닛 컨텍스트

| 항목 | 내용 |
|---|---|
| **유닛** | `u2-rag-qa` — 검색 · 답변 생성 · 근거 인용 (**MVP 핵심**) |
| **FR** | FR-14~22, FR-34, FR-35, FR-41, FR-42, FR-44 — **14건** |
| **NFR 검증** | NFR-1, 2, 5, 6, 7, 16, 19, 20, 21, 22 |
| **컴포넌트** | C30~C43 — **14종** |
| **서비스** | S3 `QueryService`, S8 `ObservabilityService` |
| **적용 스테이지** | Functional Design → **NFR Design**(프롬프트 인젝션 방어) → Code Generation → Build & Test *(Infrastructure Design·NFR Requirements 는 SKIP)* |
| **완료 시 시연** | "황산 취급 시 보호구는?" → 문장별 인용이 붙은 답변, 인용 클릭 시 원문 스니펫 하이라이트, 근거 없는 질문은 거부 + 원문 링크, LLM 사용량·비용 화면 |
| **주요 위험** | **R-4** 리랭커 지연 vs NFR-1·2 / **R-6** 안전 정보 오답 |

> ⭐ **이 유닛 완료 시점이 "단독 완결된 포트폴리오"의 최소 목표입니다.**
> SC-2(문장 단위 인용), SC-3(근거 불충분 시 거부), SC-7(LLM 사용량 조회) 이 여기서 달성됩니다.

### 이 단계에서 확정할 이월 항목

1. 하이브리드 검색 **융합식**과 후보 수 (C33)
2. **1단 거부** 임계값 산정 방식 (C39)
3. **2단 근거 지지 검증** 방식 (C40)
4. `AnswerGenerator` **구조화 출력 스키마** (C38)
5. 프롬프트 템플릿 파일 구조와 **버전 식별 규칙** (C36)
6. 청크 오프셋 → **스니펫 하이라이트** 매핑 규칙 (C41)
7. LLM 호출 추적 스키마와 비용 산정 (C13, C43)

---

## u1 실측치 — u2 설계 제약 (중요)

u1 Build & Test 및 실계약 검증에서 나온 **측정값**입니다. 추정이 아닙니다.

| 항목 | 실측 | u2 에 주는 제약 |
|---|---|---|
| 임베딩 (BGE-M3, CPU) | 청크당 **1.4~2.4초** (법령) / **0.26초** (물질) | 질의 임베딩 1건은 감당 가능하나, **리랭커를 같은 CPU 에 올리면 NFR-1(첫 토큰 5초)·NFR-2(검색 P95 2.5초) 가 위험**. R-4 가 실체화됨 |
| `app` 컨테이너 메모리 | 한도 **2 GiB**, 임베딩 모델 강제 적재 시 **85%** | 리랭커를 `app` 에 두면 초과. 워커 위임 또는 별도 서비스 필요 |
| 워커 메모리 | 한도 **4 GiB**, 모델 0.82 GiB + 인코딩 피크 2.43 GiB (batch 8) | 리랭커 추가 시 여유 **약 0.7 GiB** |
| 모델 볼륨 | **4.3 GiB** (BGE-M3 단독) | 리랭커 모델 추가분 별도 |
| BR-30 오프셋 무결성 | 실데이터 **위반 0건** (1,659청크) | **오프셋 기반 스니펫 하이라이트가 안전하다** — C41 의 전제가 검증됨 |
| 섹션 라벨 없는 문서 | MSDS 8건 중 **2건** `unstructured` (BR-20a 강등) | 인용 표시가 `section_code = NULL` 을 반드시 처리해야 함 |
| 사고사례 청크 | 레코드당 **1청크**, `section_code = NULL` (BR-31a) | 위와 동일. "사고 1건" 이 인용 단위 |
| 코퍼스 현황 | 70문서 / 1,659청크 (법령 1,140 / MSDS·물질 435 / 사고 84) | **법령이 코퍼스의 69%** — 검색 결과가 법령에 쏠릴 수 있음. 융합·필터 설계에 반영 필요 |

---

# 1부. 설계 실행 계획 (체크리스트)

### 1. 컨텍스트 로드
- [ ] 1.1 `unit-of-work.md` §u2 — 책임, DoD, 시연 산출물
- [ ] 1.2 `unit-of-work-story-map.md` §u2 — FR 14건, NFR 검증 항목
- [ ] 1.3 `application-design/components.md` — C30~C43 책임과 메서드
- [ ] 1.4 `u1-ingestion-index/functional-design/business-rules.md` — BR-20a·BR-26·BR-30·BR-31a (인용 단위의 근거)
- [ ] 1.5 `audit.md` 실계약 검증 3개 절 — u1 실측 제약

### 2. 도메인 엔터티 설계
- [ ] 2.1 질의 엔터티 (질의문, 추출 엔티티, 메타 필터)
- [ ] 2.2 검색 결과 엔터티 (후보, 스코어, 융합 순위, 리랭크 순위)
- [ ] 2.3 답변 엔터티 (문장, 인용 청크 ID 목록, 지지 판정)
- [ ] 2.4 LLM 호출 추적 엔터티 (모델, 토큰, 지연, 비용, 프롬프트 버전)
- [ ] 2.5 Enum (거부 사유, 지지 판정, 검색 모드)
- [ ] 2.6 Alembic 리비전 `0002_query` — `TraceRepo` 실테이블화 포함

### 3. 비즈니스 로직 모델 설계
- [ ] 3.1 검색 워크플로 (엔티티 추출 → 키워드·벡터 병렬 → 융합 → 리랭크 → 필터)
- [ ] 3.2 답변 생성 워크플로 (1단 거부 판정 → 컨텍스트 조립 → 구조화 생성 → 2단 검증)
- [ ] 3.3 인용 매핑 워크플로 (청크 ID → 문서·섹션·오프셋 → 스니펫)
- [ ] 3.4 사용량 집계 워크플로

### 4. 비즈니스 규칙 정의
- [ ] 4.1 검색 규칙 (후보 수, 융합식, 필터 적용 순서)
- [ ] 4.2 거부 규칙 (1단·2단 임계값, 부분 거부 처리)
- [ ] 4.3 인용 규칙 (문장당 최소 1근거, 라벨 없는 문서 표기)
- [ ] 4.4 프롬프트 규칙 (버전 고정, 데이터 영역 격리)
- [ ] 4.5 추적 규칙 (비용 산정, 비밀값 마스킹 — u1 `logging.py` 재사용)

### 5. 프론트엔드 구성 요소
- [ ] 5.1 질의 화면 (FR-44) — 입력, 스트리밍 답변, 인용 배지
- [ ] 5.2 스니펫 하이라이트 (FR-19) — 오프셋 구간 강조
- [ ] 5.3 거부 화면 (FR-20) — 거부 사유 + 원문 링크
- [ ] 5.4 면책 문구 (FR-34, FR-35)
- [ ] 5.5 사용량 화면 (FR-42)

### 6. 산출물 생성
- [ ] 6.1 `domain-entities.md`
- [ ] 6.2 `business-logic-model.md`
- [ ] 6.3 `business-rules.md`
- [ ] 6.4 `frontend-components.md`

---

# 2부. 설계 질문 (FQ2-1 ~ FQ2-20)

## A. 하이브리드 검색

### FQ2-1. 융합식 (C33, FR-15)
- **a)** RRF (Reciprocal Rank Fusion), `k=60` 고정 — 스코어 스케일 차이에 영향받지 않음 *(권장)*
- **b)** 가중 스코어 합 (정규화 후 `α·vector + (1-α)·keyword`)
- **c)** 벡터 우선, 키워드는 필터로만 사용

`[Answer]: a`

### FQ2-2. 후보 수
- **a)** 각 리트리버 30건 → 융합 후 20건 → 리랭크 후 상위 5건 *(권장)*
- **b)** 각 50건 → 융합 30건 → 리랭크 8건 (재현율 우선, 지연 증가)
- **c)** 각 15건 → 융합 10건 → 리랭크 5건 (지연 우선)

`[Answer]: a`

### FQ2-3. 법령 편중 대응 (u1 실측: 법령이 코퍼스의 69%)
- **a)** 문서 유형별 **최소 할당**을 두어 각 유형에서 최소 1~2건을 후보에 포함 *(권장)*
- **b)** 대응하지 않음 — 관련도만으로 판단
- **c)** 질의 엔티티로 유형을 추정해 필터 (예: "법적 기준" → 법령)

`[Answer]: a`

### FQ2-4. 리랭커 배치 (R-4, u1 실측: `app` 메모리 85%)
- **a)** 리랭커를 **설정으로 기본 OFF**, 켤 경우 워커에 위임하지 않고 `app` 에서 동기 실행하되 후보 수를 줄인다
- **b)** 기본 OFF + 별도 컨테이너(`reranker`) 로 분리, HTTP 호출 *(권장)*
- **c)** 기본 ON, `app` 메모리 한도를 3 GiB 로 상향

`[Answer]: b`

### FQ2-5. 질의 임베딩 캐시
- **a)** 동일 질의문 LRU 캐시 (u1 `LocalEmbeddingAdapter` 의 상한 캐시 재사용) *(권장)*
- **b)** 캐시 없음
- **c)** Redis 공유 캐시

`[Answer]: a`

## B. 거부 정책 (2단)

### FQ2-6. 1단 거부 기준 (C39, FR-20)
- **a)** 리랭크(또는 융합) 최상위 스코어가 임계값 미달이면 거부 *(권장)*
- **b)** 상위 K 평균 스코어 기준
- **c)** 상위 K 중 임계값 이상 건수가 N 미만이면 거부

`[Answer]: a`

### FQ2-7. 임계값 설정 방식 (NFR-7)
- **a)** 환경변수 + 운영 화면에서 조정, 기본값은 u4 골든셋으로 캘리브레이션 *(권장)*
- **b)** 코드 상수
- **c)** 문서 유형별 개별 임계값

`[Answer]: a`

### FQ2-8. 2단 검증 방식 (C40, FR-21)
- **a)** LLM 판정 — 각 문장과 인용 근거를 별도 호출로 지지 여부 판정 *(권장)*
- **b)** 어휘 중첩 규칙 기반 (LLM 호출 없음, 빠르지만 부정확)
- **c)** 임베딩 유사도 임계값

`[Answer]: a`

### FQ2-9. 2단 검증에서 미지지 문장 처리
- **a)** 해당 **문장만 제거**하고 나머지로 답변, 제거 사실을 표시 *(권장)*
- **b)** 답변 전체 거부
- **c)** 경고만 표시하고 문장 유지

`[Answer]: a`

### FQ2-10. 미지지 문장 제거 후 남은 문장이 없을 때
- **a)** 거부로 전환 + 원문 링크 제공 *(권장)*
- **b)** 빈 답변 반환
- **c)** 근거 목록만 제공

`[Answer]: a`

## C. 답변 생성

### FQ2-11. 구조화 출력 스키마 (C38, FR-18·19)
- **a)** `{sentences: [{text, chunk_ids: [int]}]}` — 문장별 근거 청크 ID 배열 *(권장)*
- **b)** 마크다운 본문 + 각주 번호, 별도 각주 매핑
- **c)** 문단 단위 인용

`[Answer]: a`

### FQ2-12. 문장당 근거가 0건인 경우
- **a)** 스키마 위반으로 처리하여 재시도 1회, 실패 시 해당 문장 제거 *(권장)*
- **b)** 그대로 허용 (인용 없는 문장)
- **c)** 답변 전체 거부

`[Answer]: a`

### FQ2-13. 스트리밍 (NFR-1, 첫 토큰 5초)
- **a)** 생성은 스트리밍하되 **2단 검증 후 인용을 확정**하여, 본문은 스트리밍·인용은 완료 후 부착 *(권장)*
- **b)** 검증 완료 후 일괄 반환 (스트리밍 없음)
- **c)** 인용까지 스트리밍 (검증 생략)

`[Answer]: a`

### FQ2-14. LLM 제공자
- **a)** Anthropic Claude API — `ANTHROPIC_API_KEY` 는 u1 에서 이미 설정 구조 존재 *(권장)*
- **b)** 로컬 모델 (비용 0, 품질·속도 저하)
- **c)** 설정으로 전환 가능하게 양쪽 구현

`[Answer]: a`

## D. 프롬프트 관리

### FQ2-15. 템플릿 저장 구조 (C36, NFR-22)
- **a)** `prompts/<name>/<semver>.md` + `prompts/index.yaml` 로 활성 버전 지정 *(권장)*
- **b)** 단일 파일에 버전 주석
- **c)** DB 저장

`[Answer]: a`

### FQ2-16. 프롬프트 버전 기록
- **a)** 모든 LLM 호출 추적 레코드에 `prompt_name` + `prompt_version` 저장 *(권장)*
- **b)** 답변에만 표시
- **c)** 기록하지 않음

`[Answer]: a`

## E. 인용 및 표시

### FQ2-17. 스니펫 범위 (C41, FR-19)
- **a)** 인용된 청크의 오프셋 구간 전체 + 앞뒤 문맥 없음 (BR-30 검증 완료) *(권장)*
- **b)** 청크 내에서 문장 단위로 좁힌 구간
- **c)** 청크 + 앞뒤 200자 문맥

`[Answer]: a`

### FQ2-18. 섹션 라벨이 없는 문서의 인용 표기 (u1: MSDS 2건 강등, 사고사례 전건)
- **a)** 문서 제목 + "섹션 정보 없음" 을 명시하고 원문 링크 제공 — 없는 라벨을 만들지 않는다(NFR-8) *(권장)*
- **b)** 문서 제목만 표시
- **c)** 청크 순번을 섹션처럼 표기

`[Answer]: a`

## F. 추적 및 관측

### FQ2-19. 비용 산정 (C13, FR-41, NFR-19)
- **a)** 모델별 단가를 `config/llm_pricing.yaml` 에 두고 토큰 수로 계산, 단가 미등록 모델은 **비용 NULL**(추정 금지, NFR-8) *(권장)*
- **b)** 코드 상수 단가
- **c)** 비용 기록하지 않고 토큰만

`[Answer]: a`

### FQ2-20. 질의 로그 보존 (개인정보 관점)
- **a)** 질의문 원문을 저장하되 u5 계정 연동 전까지 **소유자 미기록**, 보존 기간 설정화 *(권장)*
- **b)** 질의문 저장하지 않고 통계만
- **c)** 무기한 저장

`[Answer]: a`

---

## 답변 후 진행

1. 답변을 `audit.md` 에 기록
2. 산출물 4종 생성 → 검토 → 승인
3. 승인 시 **NFR Design** (프롬프트 인젝션 방어, NFR-16) 착수
